experiment:
  name: initial test eval

data:
  root: raw_data/chen_trajectories
  test_subdir: test
  input_length: 500
  target_length: 500
  step: 1
  decimation: 10

decimation: 10

model:
  checkpoint_path: artifacts/initial_test-20251110-192225-5a5c9199/checkpoints/epochepoch=02-val_lossval_loss=1.0686.ckpt

output:
  metrics_path: artifacts/eval/metrics.json
  forecast_dir: artifacts/eval/forecasts