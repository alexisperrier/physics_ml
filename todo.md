# TODO

- Make task to clean up plot from old runs (keep only latest)
> create a Makefile task that cleans up all plots except the most recent ones
- add plots to gitignore
- rm teacher forcing code
- explain AutoEncoder RNN
- enable: train eval continue eval continue eval